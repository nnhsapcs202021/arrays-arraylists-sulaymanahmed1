
/**
 * Write a description of class Notes here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class ArrayNotes
{
    /*
     * An array is an ordered ollection of elements of the same type - type can be primitive
     * (like an int) or a class (like turtle)
     * 
     * An array variable is like an object variable in that it must be declared and initialized
     * The number in the square backrets specificies the number of elements in the array
     * All elements in the array are initialized to their default values 
     * This code creates an array that contains 10 int values:
     */
    int[] evens = new int[10];
    /*
     * Set the value of each element in the array to the first 10 positive even integers
     * 'length' is ysed to query the number of elements in the array
     * Sq. brackets are used to reference a spec. element in the array
     * based on ints index - indices are zero based
     * 
     */
    for (int i = 0; i < evens.length; i++)
    {
        
        
    }
}
